const header=document.querySelector("[data-header]");const menuToggle=document.querySelector("[data-menu-toggle]");const cursor=document.querySelector(".cursor-orbit");const progress=document.querySelector("[data-progress]");const revealItems=document.querySelectorAll(".reveal");const magneticItems=document.querySelectorAll(".magnetic");const tickerTrack=document.querySelector(".ticker-track");const photoSlots=document.querySelectorAll(".photo-slot");if(tickerTrack){tickerTrack.innerHTML+=tickerTrack.innerHTML}const updateScroll=()=>{const max=document.documentElement.scrollHeight-window.innerHeight;const ratio=max>0?window.scrollY/max:0;header?.classList.toggle("is-scrolled",window.scrollY>18);if(progress)progress.style.transform=`scaleX(${ratio})`};window.addEventListener("scroll",updateScroll,{passive:true});updateScroll();menuToggle?.addEventListener("click",()=>{header?.classList.toggle("is-open")});document.querySelectorAll(".site-nav a").forEach(link=>{link.addEventListener("click",()=>header?.classList.remove("is-open"))});window.addEventListener("pointermove",event=>{if(cursor){cursor.style.opacity="1";cursor.style.transform=`translate(${event.clientX-120}px, ${event.clientY-120}px)`}document.documentElement.style.setProperty("--mx",`${event.clientX/window.innerWidth-.5}`);document.documentElement.style.setProperty("--my",`${event.clientY/window.innerHeight-.5}`)});magneticItems.forEach(item=>{item.addEventListener("pointermove",event=>{const rect=item.getBoundingClientRect();const x=event.clientX-rect.left-rect.width/2;const y=event.clientY-rect.top-rect.height/2;item.style.transform=`translate(${x*.035}px, ${y*.035}px)`});item.addEventListener("pointerleave",()=>{item.style.transform=""})});photoSlots.forEach(slot=>{slot.addEventListener("pointermove",event=>{const rect=slot.getBoundingClientRect();const rx=((event.clientY-rect.top)/rect.height-.5)*-5;const ry=((event.clientX-rect.left)/rect.width-.5)*5;slot.style.transform=`perspective(900px) rotateX(${rx}deg) rotateY(${ry}deg) translateY(-6px)`});slot.addEventListener("pointerleave",()=>{slot.style.transform=""})});const observer=new IntersectionObserver(entries=>{entries.forEach(entry=>{if(entry.isIntersecting){entry.target.classList.add("is-visible");observer.unobserve(entry.target)}})},{threshold:.16});revealItems.forEach(item=>observer.observe(item));
